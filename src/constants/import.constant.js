const IMPORT_LIMITS = {
  DECK_CARD_LIMIT: 500,
  MAX_IMPORT_ROWS: 1000,
  MAX_LINES_PER_FRONT: 30,
  MAX_FIELD_LENGTH: 2000,
  MAX_DISTRACTOR_LENGTH: 500,
  MAX_DISTRACTORS: 10,
  MIN_MULTIPLE_CHOICE_OPTIONS: 2,
  RAW_TEXT_MAX_LENGTH: 1_000_000,
  PREVIEW_ROWS: 5
}

const IMPORT_DELIMITER = {
  AUTO: 'auto',
  TAB: 'tab',
  COMMA: ',',
  SEMICOLON: ';',
  TAB_CHAR: '\t',
  PIPE: '|'
}

const IMPORT_DELIMITERS = Object.values(IMPORT_DELIMITER)
const OPTION_LINE_PATTERN = /^\s*(?:[A-Za-z]|\d{1,2})\s*[.)\:-]\s+(.+)$/

export { IMPORT_LIMITS, IMPORT_DELIMITER, IMPORT_DELIMITERS, OPTION_LINE_PATTERN }
